Constants

In Cangjie, variable definition uses the following structure:
Modifier Variable name: Variable type = Initial value

Mutability modifiers: let and var, which correspond to the immutable and mutable attributes, respectively. 
Mutability determines whether the value of a variable can be modified after initialization.

