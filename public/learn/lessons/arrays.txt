Arrays

Arrays are declared as Array<T> where T represents the type. T can be any type. Arrays are a reference type variables

C is initialised with 3 elements of value 0





Array d values can be initialised using lamda expressions






You can fetch size of array

This syntax allows you to assign e the values in position 1 and 2 of array d




VArrays, are value type arrays that live on the stack, reduces pressure of memory allocation and garbage collection on the heap but introduces extra performance overhead. Therefore, you are advised not to use long VArray in performance sensitive scenarios

